<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Launch-window theme only; the live UI is themed in Compose (VoiceAssistantTheme).
         NoActionBar + teal surfaces so there is no flash of a default theme on cold start. -->
    <style name="Theme.VoiceAssistant" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:statusBarColor">@color/window_background</item>
        <item name="android:navigationBarColor">@color/window_background</item>
    </style>
</resources>
